<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.UserDao">

<update id="updateUser" parameterType="User">
update User set user_name = #{user_name}, user_nickname = #{user_nickname}, user_pass = #{user_pass} , user_email = #{user_email} , user_phone = #{user_phone}, user_proPic = #{user_proPic} where user_id = #{user_id}
</update>
<select id="selectUserId" parameterType="String" resultType="User">
select*from User where user_id = #{user_id};
</select>

</mapper>