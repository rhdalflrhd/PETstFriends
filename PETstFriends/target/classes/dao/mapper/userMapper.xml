<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.UserDao">

	<select id="selectUserNn" parameterType="String"
		resultType="User">
		select * from User where user_nickname = #{user_nickname};
	</select>


	<select id="selectUserId" parameterType="String"
		resultType="User">
		select * from User where user_id = #{user_id};
	</select>

<insert id="insertPet" parameterType="pet">
		insert into pet values(
		#{user_id},0,#{pet_name},#{pet_species},
		#{pet_gender},#{pet_age}, #{pet_file}
		);
	</insert>

	<insert id="insertUser" parameterType="user">
		insert into user values(
		0,#{user_name},#{user_id},#{user_nickname},#{user_pass},#{user_email},
		#{user_phone},#{user_proPic},#{user_score},#{user_joinDate},
		#{user_adminCheck},#{user_state},#{user_pan_date}, #{user_havePet}
		);
	</insert>


</mapper>